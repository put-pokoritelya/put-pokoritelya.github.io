node_modules/
.DS_Store

# assets/hero-3d.js НЕ игнорируется намеренно: GitHub Pages отдаёт файлы
# напрямую, сборки на их стороне нет — собранный бандл должен быть в репозитории.

# Конфликтные копии iCloud: «index 2.html», «site-base 2.css».
# Появляются при массовой правке файлов и ничем не отличаются от
# промежуточного состояния оригинала — в репозиторий им нельзя.
* 2.*
