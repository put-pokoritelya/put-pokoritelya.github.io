{
  "name": "put-pokoritelya-site",
  "private": true,
  "version": "1.0.0",
  "description": "Статический сайт «Путь Покорителя». Сборка нужна только для трёхмерных блоков: она превращает src/*.js в один файл в assets/, который коммитится в репозиторий. Сам сайт остаётся статическим, GitHub Pages отдаёт файлы напрямую.",
  "scripts": {
    "build:hero": "esbuild src/hero-3d.js --bundle --minify --format=iife --target=es2018 --outfile=assets/hero-3d.js",
    "watch:hero": "esbuild src/hero-3d.js --bundle --format=iife --target=es2018 --outfile=assets/hero-3d.js --watch",
    "size": "gzip -9 -c assets/hero-3d.js | wc -c"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "three": "^0.180.0"
  }
}
